create table ACN (
	patient_mrn VARCHAR(50) NOT NULL,
	patient_admission_date DATE NOT NULL,
	ACN VARCHAR(50) NOT NULL
        PRIMARY KEY (ACN)
        FOREIGN KEY references patient_information(patient_mrn)
